{
  "name": "ipex-dashboard",
  "version": "1.0.0",
  "description": "A full-stack web application with a React + Vite frontend and an Express backend. The backend serves the frontend's static build and provides API endpoints for user authentication and order confirmation (with PDF and barcode generation).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rshngh/ipex-dashboard.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rshngh/ipex-dashboard/issues"
  },
  "homepage": "https://github.com/rshngh/ipex-dashboard#readme"
}
